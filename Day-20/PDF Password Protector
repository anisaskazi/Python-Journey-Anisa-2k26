# Day 20: PDF Password Protector
# Author: Anisa Kazi
# Concepts: File handling, PyPDF2 library, PDF encryption

from PyPDF2 import PdfReader, PdfWriter

def protect_pdf(input_pdf, output_pdf, password):
    try:
        reader = PdfReader(input_pdf)
        writer = PdfWriter()

        # Add all pages
        for page in reader.pages:
            writer.add_page(page)

        # Add password
        writer.encrypt(password)

        # Save protected PDF
        with open(output_pdf, "wb") as file:
            writer.write(file)

        print("PDF protected successfully!")

    except Exception as e:
        print("Error:", e)


if __name__ == "__main__":
    input_pdf = input("Enter input PDF file name: ")
    output_pdf = input("Enter output PDF file name: ")
    password = input("Enter password: ")

    protect_pdf(input_pdf, output_pdf, password)
